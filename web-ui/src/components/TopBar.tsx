import { Cpu, Github, Settings } from 'lucide-react';import { useAppStore } from '../store/useAppStore';
export function TopBar(){const s=useAppStore(v=>v.stats);return <div className='glass top'><div><b style={{color:'#00f7ff'}}>FScan X</b></div><div className='row'><span className='badge'>{s.online?'在线':'离线'}</span><span><Cpu size={14}/> CPU {s.cpu.toFixed(0)}%</span><span>MEM {s.memory}%</span><span>THR {s.threads}</span></div><div className='row'><Github size={18}/><Settings size={18}/><img src='https://api.dicebear.com/9.x/bottts/svg?seed=fscan' width='28'/></div></div>}
