import { useEffect, useRef } from 'react';import { Terminal } from '@xterm/xterm';import { FitAddon } from '@xterm/addon-fit';import { SearchAddon } from '@xterm/addon-search';import '@xterm/xterm/css/xterm.css';
export function TerminalView(){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(!ref.current)return;const t=new Terminal({theme:{background:'#030303',foreground:'#00f7ff'},fontSize:13,cursorBlink:true});const fit=new FitAddon();const search=new SearchAddon();t.loadAddon(fit);t.loadAddon(search);t.open(ref.current);fit.fit();let i=0;const timer=setInterval(()=>t.writeln(`\x1b[32m[${new Date().toLocaleTimeString()}]\x1b[0m scanning host 10.0.0.${++i%255}`),1100);return()=>{clearInterval(timer);t.dispose();};},[]);return <div className='glass term'><div ref={ref} style={{height:'100%'}}/></div>}
